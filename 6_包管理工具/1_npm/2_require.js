// 导入查找的方式首先会在当前文件夹下的node_modules下寻找同名的文件夹第二个是在上级目录下的node_modules中寻找同名的文件夹，直到磁盘根目录

// const uniq = require('./node_modules/uniq')

// const uniq = require('./node_modules/uniq/uniq.js')

// const uniq = require('uniq'),其实导的是当前文件夹下modules下的文件